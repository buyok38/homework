for (let b = 2; b <= 12; b = b + 2) {
    console.log(`${b}`)
}

// ====================================================================

for(let b = 10; b >= 1; b = b - 1) {
    console.log(`${b}`)
}

// ====================================================================

